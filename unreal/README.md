# Unreal Engine visualization

Placeholder. See [docs/roadmap.md](../docs/roadmap.md) -- this becomes
useful once `simulation.vehicle.Vehicle` produces simulation output worth
visualizing in real time (Phase 3+), likely via a data bridge that reads
`simulation.logger.SimulationLogger` output or streams
`simulation.rigid_body.RigidBodyState` directly.
