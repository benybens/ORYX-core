# <TITLE>

**Date:** <DATE>

## Hypothesis

What are you testing, and why? What result would validate it, and what
result would reject it? Link to a `research/hypotheses/NNNN-*.md` entry if
one exists.

## Method

What configuration, script, sweep, or bench setup was used? Reference the
exact parameters (or commit) so this is reproducible.

## Data

Raw or summarized measured results. Keep large tables/plots as separate
files in this directory (e.g. `results.csv`, `plot.png`) rather than
pasted inline.

## Conclusion

Validated / rejected / inconclusive. What changes as a result -- a new
hypothesis, a design change, a module flagged for rework? Add a
corresponding entry to `docs/research_log.md` linking back here.
